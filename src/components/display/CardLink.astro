---
interface Props {
	href: string;
	class?: string;
	[key: string]: unknown;
}

const { href, class: className, ...rest } = Astro.props;
---

<a {...rest} href={href} class:list={['displayCardLink', className]}><slot /></a>
