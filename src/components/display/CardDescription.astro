---
interface Props {
	class?: string;
	[key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<p {...rest} class:list={['displayCardDescription', className]}><slot /></p>
