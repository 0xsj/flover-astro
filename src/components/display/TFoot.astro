---
interface Props {
	class?: string;
	[key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<tfoot {...rest} class:list={['displayTableFoot', className]}><slot /></tfoot>
