---
interface Props {
	class?: string;
	[key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<dl {...rest} class:list={['displayDescriptionList', className]}><slot /></dl>
