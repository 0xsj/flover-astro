/**
 * Progress — how much of an operation has completed.
 *
 * A label is required. A null value means an unknown amount, not zero. Numeric
 * values are bounded to zero through max (100 by default), and the native
 * progress element owns its accessible semantics. The caller owns live
 * updates, completion messages, and any cancellation action.
 */
export {};
