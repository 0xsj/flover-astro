---
interface Props {
	tokens: string[];
}

const { tokens } = Astro.props;
---

<div class="swatches">
	{tokens.map((token) => (
		<div class="swatch">
			<div class="swatchChip" style={`background: var(${token})`}></div>
			<div class="swatchLabel">{token}</div>
		</div>
	))}
</div>
