---
interface Props { class?: string; [key: string]: unknown; }
const { class: className, ...rest } = Astro.props;
---

<div {...rest} role="tablist" class:list={['tabsList', className]}><slot /></div>
