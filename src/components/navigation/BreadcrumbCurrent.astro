---
interface Props {
	class?: string;
	[key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<li class="breadcrumbItem"><span {...rest} class:list={['breadcrumbCurrent', className]} aria-current="page"><slot /></span></li>
