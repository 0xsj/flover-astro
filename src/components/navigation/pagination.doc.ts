/**
 * Pagination — a one-based position in a known number of pages.
 *
 * Previous and next stop at their boundaries. First, last, and nearby pages
 * remain reachable with noninteractive ellipses for gaps. One page or zero
 * pages renders no navigation. Astro emits links so the route layer owns the
 * URL and data change; use `hrefForPage` to compose that route.
 */
export {};
