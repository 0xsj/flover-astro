/**
 * Tabs — one panel at a time, with a keyboard contract that is easy to omit.
 *
 * The strip is one tab stop: Tab enters it and leaves it, while Left/Right,
 * Home, and End move between tabs. Automatic activation follows the sibling
 * default; manual activation moves focus without changing the panel until
 * Enter or Space commits it.
 */
export {};
