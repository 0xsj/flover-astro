/**
 * Workspace renderers own geometry and accessible interaction affordances;
 * callers own nodes, widgets, selection, and persistence. Narrow-screen
 * stacking is visual and does not rewrite the saved desktop layout.
 */
export {};
