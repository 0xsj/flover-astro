---
interface Props {
	for?: string;
	class?: string;
	[key: string]: unknown;
}

const { for: htmlFor, class: className, ...rest } = Astro.props;
---

<label {...rest} for={htmlFor} class:list={['formLabel', className]}><slot /></label>
