---
interface Props { class?: string; [key: string]: unknown; }
const { class: className, ...rest } = Astro.props;
---

<div {...rest} class:list={['accordionContent', className]} role="region" data-accordion-content><div class="accordionInner"><slot /></div></div>
