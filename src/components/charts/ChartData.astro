---
import '../../styles/charts.css';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<details class="chartData">
	<summary>View data for {title}</summary>
	<div class="chartDataTable" role="region" aria-label={`Data for ${title}`} tabindex="0"><slot /></div>
</details>
