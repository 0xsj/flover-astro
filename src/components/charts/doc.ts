/**
 * The Astro chart family owns ordinary metric charts, statistical marks, and a
 * dependency-free graph frame. Network presets compose over deterministic
 * layouts without making the frame responsible for a chart engine.
 */
export {};
