---
interface Props {
	delay?: number;
	class?: string;
}

const { delay = 300, class: className } = Astro.props;
---

<div class:list={['tooltipProvider', className]} data-astro-tooltip-provider data-tooltip-delay={delay}><slot /></div>
