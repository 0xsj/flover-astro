---
interface Props {
	side?: 'top' | 'right' | 'bottom' | 'left';
	align?: 'start' | 'center' | 'end';
	sideOffset?: number;
	class?: string;
	id?: string;
}

const { side = 'bottom', align = 'start', sideOffset = 6, class: className, id = 'astro-popover-content' } = Astro.props;
---

<div class="overlayLayer" data-popover-layer hidden>
	<div
		id={id}
		class:list={['floatingPanel', className]}
		role="dialog"
		tabindex="-1"
		data-popover-panel
		data-side={side}
		data-align={align}
		data-side-offset={sideOffset}
	>
		<slot />
	</div>
</div>
