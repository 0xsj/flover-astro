/**
 * DropdownMenu — a list of actions, not a Select.
 *
 * A menu has no current value, submits nothing, and reopens with no item marked
 * selected. Arrow keys, Home, End, Escape, and typeahead move through the
 * enabled action buttons. Highlight is mirrored with `data-highlighted` so
 * keyboard movement is visible without relying on `:hover`.
 */
export {};
