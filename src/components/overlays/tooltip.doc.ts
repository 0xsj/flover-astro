/**
 * Tooltip — a supplementary hint for a trigger that already has a name.
 *
 * It opens on hover and focus, but cannot be the only accessible name because
 * touch users and people who never hover cannot reach it. Nothing interactive
 * belongs inside: a control or a longer-lived explanation is a Popover.
 * `TooltipProvider` owns the shared delay for a screen or subtree.
 */
export {};
