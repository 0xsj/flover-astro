/**
 * Presentation formatting receives locale, time zone, and currency explicitly.
 * Construction validates the context and returns Result; invalid values never
 * silently fall back to the host machine. Formatting is presentation only:
 * currency conversion and calendar-date coercion belong to other domains.
 */
export {};
