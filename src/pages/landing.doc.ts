/**
 * Public entry contract: `/` introduces Flover as an Astro template and stays
 * at the root address. A short description leads to the currently ported
 * kitchen sink and token catalog without requiring an account or backend read.
 *
 * The landing reuses the existing type, button, and layout primitives with the
 * sibling theme-aware two-radial-gradient treatment. The semantic accent token
 * supplies Astro's lavender bloom; the geometry and light-theme attenuation
 * remain the sibling contract.
 *
 * Cookbook and manual destinations are still future port work, so the current
 * CTAs point only at routes that exist in this Astro slice.
 */
export {};
